server:
  port: 8080
  host: "0.0.0.0"

database:
  host: "postgres"
  port: "5432"
  user: "postgres"
  password: "postgres_password"
  dbname: "postgres"
  sslmode: "disable"

logging:
  level: "info"
  format: "json"